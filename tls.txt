FOR TESTING PURPOSES ONLY!!
To generate self certified tls certificate (tested on Linux), run following commands:
`cd tls/`
`go run /usr/local/go/src/crypto/tls/generate_cert.go --rsa-bits=2048 --host=localhost`
